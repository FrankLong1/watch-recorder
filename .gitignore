# Secrets. Never commit these — the OpenAI key and the ingest token are the two
# things in this project that cannot be rotated silently.
.env
.env.*
!.env.example

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/
.DS_Store

# Swift
.swiftpm/
.build/

# Backend build and provider caches. Lockfiles remain tracked for reproducible
# installs and Terraform provider selection.
server/node_modules/
server/dist/
server/terraform/.terraform/
