# Real values live in terraform.tfvars, which is gitignored. This repo is
# public, so nothing here names actual infrastructure.
#
# `project_id`, `region` and `sql_instance_name` are deliberately absent: the
# README exports them as TF_VAR_project_id, TF_VAR_region and
# TF_VAR_sql_instance_name alongside the PROJECT_ID/REGION/SQL_INSTANCE that
# every later gcloud command reads, so one shell configures the whole deploy.
# Naming them here would defeat that — a -var-file outranks the environment,
# so a stale placeholder would silently win over the values you just set.

# Left empty on the first apply so the database, identity, secrets and image
# repository are created before there is anything to deploy. Fill in the digest
# emitted by `gcloud builds submit` and apply again.
image = ""

# Human IAM user allowed to impersonate the migrator when running migrations.
# Required — there is no default.
operator_iam_user = "you@example.com"

openai_model = "gpt-4o-transcribe"
user_id      = "frank"
